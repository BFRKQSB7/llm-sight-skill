# llm-sight 维护文档

## 项目概览与目标

Claude Code skill：给**非多模态大模型一双"眼睛"**——用 PP-OCRv6（rapidocr + onnxruntime，GPU 走 DirectML）把图片/截图/扫描件的文字提取成纯文本。**边界**：只管 OCR（图片→文字），不做图像理解、不做对话。模型三版本：`_lite`（不带模型）/ `_standard`（内置 v6 small）/ `_full`（内置 small+medium + GPU 模块）。

## 维护状态

个人维护项目，按需更新，PR 欢迎但不保证及时。**版本号唯一权威在 `VERSION` 文件**（现为 `2.0.0`），README 徽章/变更日志/发布 zip 均须与其一致——改版本先改 `VERSION`。

## 构建 / 运行 / 测试

- **安装**：skills 目录放 `llm-sight`；有 Python（3.8–3.13）即自动建 venv/装依赖/推荐下载模型
- **运行**：`scripts/ocr.py <图片>` → 纯文本（也可 JSON 带置信度/坐标）；斜杠命令 `/llm-sight-status` 查环境
- **打包**：`scripts/package.sh` 产 `dist/llm-sight-{版本}\_{full,standard,lite}.zip`（三版本）。**历史坑**：曾解压到 STAGE 子目录导致 v1 空 zip
- **测试**：无自动化测试；改环境引导/模型逻辑后手动跑 `setup.py` + `ocr.py` 各路径
- **首用引导**：新装（无 venv）先 `/llm-sight-status` + setup 再 OCR；误删恢复时模型可 `/llm-sight-model` 重装

## 目录结构 / 代码地图

| 路径 | 职责 |
|------|------|
| `VERSION` | **版本权威**（`2.0.0`；README/zip 都对齐它） |
| `SKILL.md` | 主 skill：触发规则、OCR 调用链、部署说明 |
| `scripts/ocr.py` | 主程序：图片→文字（GPU/CPU 模型选择，默认未装回退已装最佳） |
| `scripts/setup.py` | 环境引导：venv/依赖（走配置代理）/模型下载 |
| `scripts/package.sh` | 三版本打包（dist/ zip；先清 STAGE 再解压） |
| `scripts/models.json` | 模型候选清单（体积/识别率/语言/适合人群） |
| `commands/` | 斜杠命令说明：`llm-sight-config.md`（交互面板）/model/status/update/help |
| `references/troubleshooting.md` | 排错（venv 缺失/模型缺失/GPU 模块冲突） |
| `dist/` | 发布 zip **产物**（_full/_standard/_lite，入 git 供 Releases 直用） |
| `docs/en/` | 英文 README |
| `LICENSE` | MIT |

## 发布流程（对齐 improve skill）

1. 改版本号：**先改 `VERSION` 文件**，再同步 README 标题/徽章/变更日志 + `scripts/package.sh` 产物名，**改版本必须 `grep X.Y.Z` 全改**，改完复查零残留
2. README「变更日志」节顶部加新版本段（分类词：新增/修复/删除/优化/版本），中英 README 同步
3. 重打包三个 dist zip（确保非空、含正确模型档）后提交
4. commit（作者 `226671264+BFRKQSB7@users.noreply.github.com`）→ tag `v{版本号}` → push → `gh release create`（附三 zip）

## 提交规范

Conventional Commits 轻量 5 前缀：`feat` / `fix` / `docs` / `refactor` / `chore`。小更新+距上次不久可覆盖发布（重打同 tag）。

## 版本纪律

semver：破坏性→x+1、新功能→y+1、修 bug→z+1。**tag 必须与 `VERSION` 文件同步**（v2.0.0 曾文档优先、tag 滞后，现 VERSION=2.0.0 对齐）。历史 tag：v1.0.1、v2.0.0。

## 本仓库特有坑 / 注意

- **GPU 模块装包冲突**：`install_gpu_module` 必须**先卸载 onnxruntime 再装 directml**——两包同名模块并存会 import 混乱
- **50 系显卡**：DirectML 走 D3D12 在 NVIDIA RTX 50（Blackwell）可用；paddle CUDA 路线在 50 系不可用（弃用 paddle）
- 模型默认未装 → 自动回退已装最佳模型（GPU/CPU 皆适用），改动模型选择逻辑时保留这层回退
- pip 依赖下载必须走配置代理（venv 构建时 hf 等被墙）